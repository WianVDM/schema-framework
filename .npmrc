# NOTE: Uncomment the line below when enabling npm publishing (target: v1.0.0)
# Also add NPM_TOKEN as a GitHub Actions secret and pass it as env var in release.yml
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
