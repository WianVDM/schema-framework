export interface ThemeConfig {
  classes?: Partial<{
    grid: string
    gridRow: string
    gridCell: string
    gridHeader: string
    form: string
    formField: string
    submitButton: string
    cancelButton: string
    pagination: string
    toolbar: string
  }>
}