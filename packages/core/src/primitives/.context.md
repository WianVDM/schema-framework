# Primitives Layer - Context Map

Layer 1: Generic UI wrappers with no schema knowledge. Imports only React and shadcn/ui.

## File Inventory

| File | Export Name | Export Type | Description |
|------|-------------|-------------|-------------|
| address-input.tsx | AddressData, AddressPlaceholders, AddressInput | interface, interface, function | Address entry component with structured fields |
| file-upload.tsx | FileUpload | function | File upload component |
| status-badge.tsx | StatusBadge | function | Status indicator badge component |
| index.ts | *(barrel)* | re-export | Re-exports all primitives |

## Internal Relationships

- `index.ts` <-- re-exports --> `address-input.tsx`
- `index.ts` <-- re-exports --> `file-upload.tsx`
- `index.ts` <-- re-exports --> `status-badge.tsx`

## External Dependencies

- None (Layer 1 imports only React and external UI libraries)

## Violations (Pre-Rule)

- `address-input.tsx` exports both `AddressData` (interface) and `AddressInput` (function) - should be split into `address-data.ts` and `address-input.tsx`